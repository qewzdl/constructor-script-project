{
	email {$SITE_EMAIL}
}

{$SITE_DOMAIN} {
        encode gzip zstd
        reverse_proxy api:8080
        header {
                Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        }
}
