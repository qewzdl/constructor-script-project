{
	email {$SITE_EMAIL}
}

{$SITE_DOMAIN} {
	encode gzip zstd

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
	}

	# UPLOADS
	handle_path /uploads/* {
		root * /app/uploads
		try_files {path}
		file_server
	}

	# API (including static files served by Go app)
	handle {
		reverse_proxy api:8080
	}
}
